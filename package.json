{
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:localhost": "hardhat run --network localhost scripts/deploy-localhost.js",
    "deploy:sepolia": "hardhat run --network sepolia scripts/deploy-sepolia.js",
    "deploy:holesky": "hardhat run --network holesky scripts/deploy.js",
    "node": "hardhat node",
    "dev:setup": "hardhat run --network localhost scripts/start-dev.js",
    "seed": "hardhat run --network localhost scripts/seed-data.js",
    "verify:sepolia": "hardhat verify --network sepolia",
    "verify:holesky": "hardhat verify --network holesky"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.9.0",
    "@types/node": "^24.0.4",
    "hardhat": "^2.24.3",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "@openzeppelin/contracts-upgradeable": "^5.3.0",
    "dotenv": "^16.5.0"
  }
}
